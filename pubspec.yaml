name: input_event
description: A starting point for Dart libraries or applications.
version: 1.0.0
repository: https://github.com/smartrent/dart_input_event.git

environment:
  sdk: ^3.4.3

# Add regular dependencies here.
dependencies:
  collection: ^1.19.1
  # path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
